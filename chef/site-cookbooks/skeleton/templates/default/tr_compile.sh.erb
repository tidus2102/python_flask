#!/bin/sh

. <%= node[:skeleton][:python][:virtualenv] %>/bin/activate

set -e

bin_dir="$(cd "$(dirname "$0")/.."; pwd)"
cd "$bin_dir"

pybabel compile -f -d <%= node[:skeleton][:app_name] %>/translations